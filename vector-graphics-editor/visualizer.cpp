#include "visualizer.h"

#include "renderer_impl.h"

void Renderer::drawPoint() {
    auto do_stuff = [] ([[maybe_unused]]RendererImpl* renderer) {
    };

    do_stuff(renderer_);
}

void Renderer::drawLine() {
    auto do_stuff = [] ([[maybe_unused]]RendererImpl* renderer) {
    };

    do_stuff(renderer_);
}
